<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lecboxyazilim.lecbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".RandevuActivity"></activity>
        <activity android:name=".Transportation2Activity"></activity>
        <activity android:name=".TransportationActivity" />
        <activity android:name=".MuhendislikHocalarActivity" />
        <activity android:name=".FenEdebiyatActivity" />
        <activity android:name=".IsletmeActivity" />
        <activity android:name=".IktisatFakultesiActivity" />
        <activity android:name=".MuhendislikFakultesiActivity" />
        <activity android:name=".FakulteActivity" />
        <activity android:name=".TeacherFinder" />
        <activity android:name=".SignInActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>